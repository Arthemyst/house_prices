name: First Workflow
on: [push,workflow_dispatch]
jobs:
  first-job:
    runs-on: self-hosted

    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Create text file
        run: |
          echo "This is the content of the file" > file.txt
          mv file.txt $GITHUB_WORKSPACE/
